name: genshin_chat
description: "纯本地原神角色聊天 Android 应用。"
publish_to: 'none'
version: 2.1.0+21

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/data/characters.json
